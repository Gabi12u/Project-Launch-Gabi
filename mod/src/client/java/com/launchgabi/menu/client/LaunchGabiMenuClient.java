package com.launchgabi.menu.client;

import net.fabricmc.api.ClientModInitializer;

/**
 * Entry point for the client-only "modern main menu" mod. Everything visual
 * happens through the mixin package; nothing needs to run at start-up yet.
 */
public class LaunchGabiMenuClient implements ClientModInitializer {
	@Override
	public void onInitializeClient() {
	}
}
